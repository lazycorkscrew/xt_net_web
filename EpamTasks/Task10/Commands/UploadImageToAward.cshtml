@using EpamTasks.Entities;
@using EpamTasks.PL;

@{
    Layout = null;
    int id = int.Parse(Request["id"]);
    if (IsPost)
    {
        byte[] image = WebImage.GetImageFromRequest().GetBytes() ?? null;
        if (image != null)
        {
            LogicProvider.UserLogic.SetImageByAwardId(id, image);
            Response.Redirect($"~/?page=award&id={id}");
        }
    }

    <div>Что-то пошло не так.</div>
}


